name: My First Github Action
on: [push]
jobs: 
    print-hello-world:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: actions/setup-node@v2
                with:
                    mode-version: '14.17.0'
            - run: node -v

